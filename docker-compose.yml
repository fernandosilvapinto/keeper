name: keeper

services:
  keeper-db:
    image: postgres:16-alpine
    container_name: keeper-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: ${KC_DB_PASSWORD}
    volumes:
      - keeper-db-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U keycloak -d keycloak"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - keeper

  keeper:
    image: quay.io/keycloak/keycloak:26.7
    container_name: keeper
    restart: unless-stopped
    command: ["start-dev", "--http-port=8081"]
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KC_ADMIN_USER}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KC_ADMIN_PASSWORD}
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keeper-db:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KC_DB_PASSWORD}
      KC_HOSTNAME: http://keeper.localtest.me:8081
      KC_HOSTNAME_STRICT: "false"
      KC_HTTP_ENABLED: "true"
      KC_HEALTH_ENABLED: "true"
      KC_METRICS_ENABLED: "true"
      KC_LOG_LEVEL: info
    ports:
      - "8081:8081"
      - "9000:9000"
    volumes:
      - ./themes:/opt/keycloak/themes
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      keeper-db:
        condition: service_healthy
    networks:
      - keeper

  openldap:
    image: osixia/openldap:1.5.0
    container_name: keeper-ldap
    profiles:
      - federation
    environment:
      LDAP_ORGANISATION: Keeper Lab
      LDAP_DOMAIN: keeper.local
      LDAP_ADMIN_PASSWORD: ${LDAP_ADMIN_PASSWORD}
    volumes:
      - ldap-data:/var/lib/ldap
      - ldap-config:/etc/ldap/slapd.d
    ports:
      - "1389:389"
    networks:
      - keeper

  phpldapadmin:
    image: osixia/phpldapadmin:0.9.0
    container_name: keeper-ldapadmin
    profiles:
      - federation
    environment:
      PHPLDAPADMIN_LDAP_HOSTS: openldap
      PHPLDAPADMIN_HTTPS: "false"
    ports:
      - "8083:80"
    depends_on:
      - openldap
    networks:
      - keeper

  openfga:
    image: openfga/openfga:v1.12.0
    container_name: keeper-openfga
    profiles:
      - authz
    command: run
    environment:
      OPENFGA_PLAYGROUND_ENABLED: "true"
    ports:
      - "8084:8080"
      - "3001:3000"
    networks:
      - keeper

volumes:
  keeper-db-data:
  ldap-data:
  ldap-config:

networks:
  keeper:
    name: keeper-net
